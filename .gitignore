cpp/build*/
*.db
*.db-wal
*.db-shm

.omc/
*.key
cpp/build/
.pantheon/
.cache/
.repowise/
.vscode/
.mcp.json
compile_commands.json
.claude/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# local tool cache and one-off analysis scripts, machine-specific paths
.impeccable/
bench/*.py
